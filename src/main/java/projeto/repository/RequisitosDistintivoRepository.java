package projeto.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import projeto.model.entity.RequisitosDistintivo;

public interface RequisitosDistintivoRepository extends JpaRepository<RequisitosDistintivo, Integer> {
}
